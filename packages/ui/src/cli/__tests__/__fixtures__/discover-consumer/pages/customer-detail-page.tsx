// A page importing the shadow HeaderPage — gives discover an importedBy
// count to assert against. The minimal body keeps the focus on the import.

import { HeaderPage } from "../components/ui/header-page"
import { LifetimeMetrics } from "../components/lifetime-metrics"

export default function CustomerDetailPage() {
  void HeaderPage
  void LifetimeMetrics
  return null
}
